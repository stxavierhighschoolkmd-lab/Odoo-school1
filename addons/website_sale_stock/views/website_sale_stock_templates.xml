<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template
        id="website_sale_stock.website_sale_stock_cart_lines"
        inherit_id="website_sale.cart_lines_quantity"
        name="Shopping Cart Lines"
    >
        <xpath expr="//input[@type='text'][hasclass('quantity')]" position="attributes">
            <attribute name="t-att-data-max">line._get_max_line_qty()</attribute>
        </xpath>
        <xpath expr="//div[contains(@t-attf-class, 'css_quantity')]" position="after">
            <div class="availability_messages"/>
        </xpath>
    </template>

    <template
        id="website_sale_stock.website_sale_stock_product"
        inherit_id="website_sale.cta_wrapper"
        priority="4"
    >
        <xpath expr="//div[@id='o_wsale_cta_wrapper']" position="before">
            <div class="availability_messages o_not_editable"/>
            <input id="wsale_user_email" type="hidden" t-att-value="user_email"/>
        </xpath>
    </template>

    <template
        id="website_sale_stock.ribbon_filter_in_stock"
        inherit_id="website_sale.filter_products_ribbons"
        name="In Stock Ribbon Filter"
    >
        <xpath expr="//div[hasclass('ribbon-filters')]" position="inside">
            <t t-if="show_in_stock_filter">
                <t t-set="_active" t-value="ribbon_filter == 'in_stock'"/>
                <input
                    type="checkbox"
                    name="attribute_value"
                    class="btn-check"
                    t-att-id="in_stock"
                    t-att-value="in_stock"
                    t-att-checked="_active"
                    autocomplete="off"
                    t-attf-onclick="location.href='#{keep(ribbon=0 if _active else 'in_stock')}';return false;"
                />
                <label
                    t-attf-class="btn border {{'active bg-primary-subtle border-primary text-primary-emphasis' if _active else ''}}"
                    t-att-for="in_stock"
                    t-field="In stock"
                />
            </t>
        </xpath>
    </template>

</odoo>
