<templates xml:space="preserve">
    <t t-name="html_editor.ListSelector">
        <Dropdown menuClass="'o-we-toolbar-dropdown'" bottomSheet="false" menuRef="this.menuRef">
            <button class="btn btn-light oi" data-icon="format_list_bulleted" t-att-title="this.props.title" name="list_selector"/>
            <t t-set-slot="content" t-key="this.props.key.value">
                <div data-prevent-closing-overlay="true">
                    <t t-set="activeMode" t-value="this.getActiveMode()"/>
                    <t t-foreach="this.props.getButtons()" t-as="button" t-key="button_index">
                        <button
                            t-att-title="button.description"
                            t-att-name="button.id"
                            class="btn btn-light oi"
                            t-att-class="{ active: activeMode === button.mode }"
                            t-att-data-icon="button.icon"
                            t-on-click="button.run"
                        />
                    </t>
                </div>
            </t>
        </Dropdown>
    </t>
</templates>
